import { Page } from '@/widgets/Page';
import { FirstSection } from '@/entities/FirstSection';

const MainPage = () => {
    return (
        <Page>
            <FirstSection />
        </Page>
    );
};

export default MainPage;
